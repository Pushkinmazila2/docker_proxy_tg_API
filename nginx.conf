events {}
http {
    server {
        listen 80;
        
        location /${{ secrets.MYSECRET_WAY }}/(.*) {
            proxy_pass https://api.telegram.org;
            proxy_ssl_server_name on;
        }
    }
}